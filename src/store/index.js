import Vue from 'vue'
import Vuex from 'vuex'

// Modules
import auth from './auth.module'

Vue.use(Vuex)

export default new Vuex.Store({
  modules: {
    auth
  },
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
